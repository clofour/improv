services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000

  db:
    image: postgres:trixie
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres